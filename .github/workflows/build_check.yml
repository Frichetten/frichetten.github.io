name: Build Check

on:
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v2
        
      - name: Install Hugo
        run: sudo apt install -y hugo
        
      - name: Build Website
        run: hugo --minify
